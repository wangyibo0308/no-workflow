name: Hello on Push (sleep 3m)
on:
  push:
    branches: [main]

jobs:
  hello_sleep:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "Hello"
      - name: Sleep
        run: sleep 180
